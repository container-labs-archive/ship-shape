// @flow

const styles = theme => ({
  container: {
    marginBottom: theme.spacing(2),
    marginTop: theme.spacing(2),
  },
  typeButton: {
    borderRadius: '0px',
    boxShadow: 'none',
  },
});

export default styles;
