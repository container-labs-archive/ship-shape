const styles = theme => ({
  field: {
    maxWidth: 720,
    display: 'flex',
    marginBottom: theme.spacing(2),
  },
  dropzone: {
    marginTop: theme.spacing(6),
    marginBottom: theme.spacing(2),
  },
  buttons: {
    marginTop: theme.spacing(6),
  },
});

export default styles;
